/**
 * main.js - Client-side logic for smartComps
 * 
 * TODO: 
 * 1. Fetch /api/config on load to populate UI.
 * 2. Handle 'Train Model' click (POST /api/train).
 * 3. Handle 'Get Prediction' click (POST /api/predict).
 */

console.log("smartComps dashboard loaded.");

