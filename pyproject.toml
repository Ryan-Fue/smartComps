[project]
name = "smartcomps"
version = "0.1.0"
package-mode = false
description = ""
authors = [
    {name = "Ryan-Fue",email = "ryanfue@ucla.edu"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "yfinance (>=1.2.0,<2.0.0)",
    "pandas (>=3.0.1,<4.0.0)",
    "scikit-learn (>=1.8.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)"
]

